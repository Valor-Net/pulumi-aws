name: Update ECS Service Image

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Image tag to deploy'
        required: true
      service_name:
        description: 'Service name'
        required: true
      stack:
        description: 'Stack name'
        required: true
      commit_sha:
        description: 'Commit SHA'
        required: true
      ecr_repository:
        description: 'ECR Repository'
        required: true
      ecr_nginx_repository:
        description: 'ECR Nginx Repository'
        required: true
      ecs_cluster:
        description: 'ECS Cluster'
        required: true
      pulumi_service_name:
        description: 'Pulumi Service Name'
        required: true

jobs:
  update-service:
    runs-on: ubuntu-latest
    environment: staging
    name: Run ECS update - ${{ github.event.inputs.stack }} - ${{ github.event.inputs.service_name }}
    
    steps:
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        persist-credentials: true 
      
    - name: Setup Pulumi
      uses: pulumi/actions@v4
      with:
        pulumi-version: ^3.0.0
        
    - name: Update Pulumi config and ECS Service
      env:
        PULUMI_CONFIG_PASSPHRASE: ""
        PULUMI_BACKEND_URL: ${{ secrets.PULUMI_BACKEND_URL }}
      run: |
        echo "🔄 Atualizando imagem do serviço: ${{ github.event.inputs.service_name }}"
        echo "🏷️ Nova imagem tag: ${{ github.event.inputs.image_tag }}"
        
        # Definir variáveis
        CLUSTER_NAME="${{ github.event.inputs.ecs_cluster }}"
        SERVICE_NAME="${{ github.event.inputs.service_name }}"
        ECR_REPOSITORY="${{ github.event.inputs.ecr_repository }}"
        IMAGE_TAG="${{ github.event.inputs.image_tag }}"
        STACK_NAME="${{ github.event.inputs.stack }}"
        
        # Construir a URI completa da imagem
        AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
        IMAGE_URI="${AWS_ACCOUNT_ID}.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/${ECR_REPOSITORY}:${IMAGE_TAG}"
        
        echo "🐳 Nova imagem: ${IMAGE_URI}"
        
        # ========================================
        # 1. ATUALIZAR CONFIG DO PULUMI
        # ========================================
        echo "⚙️ Atualizando configuração do Pulumi..."
        
        # Selecionar stack
        pulumi stack select ${STACK_NAME}
        
        CONFIG_KEY="valornet-infra:${{github.event.inputs.pulumi_service_name}}.imageTag"
        
        echo "📝 Atualizando config: ${CONFIG_KEY} = ${IMAGE_TAG}"
        pulumi config set ${CONFIG_KEY} ${IMAGE_TAG}
        
        echo "✅ Configuração do Pulumi atualizada!"
        
        # ========================================
        # 2. ATUALIZAR ECS SERVICE
        # ========================================
        echo "🚀 Atualizando serviço ECS..."

        echo "cluster: ${{github.event.inputs.ecs_cluster}}"
        echo "service: ${{github.event.inputs.service_name}}"
        
        # Obter a task definition atual
        echo "📋 Obtendo task definition atual..."
        TASK_DEFINITION_ARN=$(aws ecs describe-services \
          --cluster ${{github.event.inputs.ecs_cluster}} \
          --services ${{github.event.inputs.service_name}} \
          --query 'services[0].taskDefinition' \
          --output text)
        
        echo "📄 Task Definition ARN: ${TASK_DEFINITION_ARN}"
        
        # Baixar a task definition atual
        aws ecs describe-task-definition \
          --task-definition ${TASK_DEFINITION_ARN} \
          --query 'taskDefinition' \
          --output json > task-definition.json
        
        # Atualizar a imagem na task definition
        echo "🔧 Atualizando imagem na task definition..."
        
        # Usar jq para atualizar a imagem (assumindo que é o primeiro container)
        jq --arg IMAGE_URI "${IMAGE_URI}" \
          '.containerDefinitions[0].image = $IMAGE_URI | 
           del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .placementConstraints, .compatibilities, .registeredAt, .registeredBy)' \
          task-definition.json > new-task-definition.json
        
        echo "📋 Nova task definition:"
        cat new-task-definition.json | jq '.containerDefinitions[0].image'
        
        # Registrar nova task definition
        echo "📝 Registrando nova task definition..."
        NEW_TASK_DEFINITION_ARN=$(aws ecs register-task-definition \
          --cli-input-json file://new-task-definition.json \
          --query 'taskDefinition.taskDefinitionArn' \
          --output text)
        
        echo "✅ Nova task definition registrada: ${NEW_TASK_DEFINITION_ARN}"
        
        # Atualizar o serviço com a nova task definition
        echo "🚀 Atualizando serviço..."
        aws ecs update-service \
          --cluster ${CLUSTER_NAME} \
          --service ${SERVICE_NAME} \
          --task-definition ${NEW_TASK_DEFINITION_ARN} \
          --force-new-deployment
        
        
        # ========================================
        # 3. REFRESH PULUMI STATE (SINCRONIZAR COM REALIDADE)
        # ========================================
        echo "🔄 Executando Pulumi refresh para sincronizar estado..."
        echo "📝 Isso vai atualizar o state file com as mudanças feitas diretamente no AWS"
        
        # Fazer refresh do estado para evitar drift
        if pulumi refresh --yes --stack "${STACK_NAME}"; then
          echo "✅ Pulumi state atualizado com sucesso!"
          echo "📊 Estado sincronizado entre Pulumi e AWS"
        else
          echo "⚠️ Aviso: Falha no pulumi refresh, mas deployment foi concluído"
          echo "💡 Considere executar 'pulumi refresh' manualmente depois"
        fi

        # ========================================
        # 4. COMMIT MUDANÇAS DO PULUMI
        # ========================================
        echo "💾 Fazendo commit das mudanças na configuração..."
        
        # Verificar se há mudanças
        if git diff --quiet; then
          echo "ℹ️ Nenhuma mudança para commit"
        else
          git config --global user.email "ci@valornet.com"
          git config --global user.name "CI/CD Bot"
          
          git add Pulumi.${STACK_NAME}.yaml
          git commit -m "ci: update ${SERVICE_NAME} imageTag to ${IMAGE_TAG}"
          
          # Push mudanças
          git push origin main
          
          echo "✅ Configuração commitada e enviada!"
        fi
        
        echo "🎉 Processo concluído com sucesso!"
        echo "📊 Resumo:"
        echo "  - Config Pulumi: ✅ Atualizada"
        echo "  - ECS Service: ✅ Atualizado"
        echo "  - Serviço: ${SERVICE_NAME}"
        echo "  - Nova imagem: ${IMAGE_TAG}"